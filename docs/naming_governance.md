# Naming Governance

## 规则

1. 正式文件、目录、模块、配置键和字段使用 `snake_case`。
2. 名称必须表达职责、机制或业务含义。
3. 禁止用数字阶段名、弱版本后缀、`new`、`old`、`best`、`final` 表示正式语义。
4. 技术版本可以出现在依赖声明或环境说明中，但不应作为项目阶段名。

## 推荐示例

```text
record_writer
artifact_manifest
release_readiness_gate
claim_audit_table
```

# VidSig 官方 checkpoint、generate_ms.py 与 attack.py wrapper 运行依赖。
# checkpoint 仍必须来自官方公开资源或项目内 official resource bootstrap。
# 官方 requirements 会固定 torch / torchvision, 但 Colab 已预装 CUDA 可用版本。
# 因此这里不重装 torch / torchvision, 只安装官方生成与检测直接需要的依赖。
gdown
numpy
imageio
imageio-ffmpeg
opencv-python
augly==1.0.0
omegaconf
pandas
Pillow
tqdm
diffusers
transformers
accelerate
safetensors
